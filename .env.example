# Copy to .env for local development (see .cursor/README.md)
DATABASE_URL=postgresql://umami:umami@localhost:5432/umami
APP_SECRET=local-dev-secret-change-in-production

# Optional: set to 1 during .cursor/setup-dev-env.sh to load demo analytics (pnpm seed-data)
# UMAMI_SEED_DATA=0
